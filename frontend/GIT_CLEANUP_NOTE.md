# Git cleanup note

This repository was updated to ensure `frontend/node_modules/` and `frontend/build/` are **ignored** and **untracked** by git (local files kept).
A single commit was created with message:

`Frontend stabilization: deps cleanup, lockfile regen, Node 20 engine markers`
